// Copyright 2023 Jesse Caro

#include<iostream>  // header file for I/O objects cin, cout, etc
using std::cout;
//  using namespace std; - we'll NEVER do this because it makes everything
//  in the std namespace availabe. We'll just place a using statement
//  for the things that we're using

int main() {
//  std::cout << "Hello World!";
  cout << "Hello World!";
  return 0;  // 0 denotes successful program completion
}

